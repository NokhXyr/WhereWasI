# Gradle
.gradle/
classes/
moddev/
resources/
tmp/

# Local build artifacts (per-machine)
# /release/ holds the current build (see packageRelease); build/libs accumulates one jar
# per build number. Published jars live on GitHub Releases, so none of this belongs in git.
/release/
build_number.txt
build/libs/*.jar

# ModDevGradle run directories
run/
runs/

# IDEs
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
bin/
out/
eclipse/

# Logs & crash reports
logs/
crash-reports/
*.log

# OS cruft
.DS_Store
Thumbs.db
